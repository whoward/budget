

= f.check_box :active
= f.text_field :name

- if f.object.new_record?
  = f.collection_select :type, Budget::ImportService.registry, :name, :description
- else
  = render f.object.class.name.underscore, f: f

.form-actions
  = f.submit action, class: 'btn btn-primary'
